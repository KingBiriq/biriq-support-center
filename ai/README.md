# AI Folder Guide

- `prompts/` contains prompts passed directly to the model.
- `knowledge_base/` contains company rules and support knowledge.
- `schemas/` contains suggested structured outputs and tool contracts.
- `examples/` contains test conversations and QA checks.

The AI must use live database data for customer-specific questions. Markdown files define policy; they are not a replacement for database integration.
